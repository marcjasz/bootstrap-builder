package pl.put.poznan.builder.logic.test;

import org.junit.Before;
import org.testng.annotations.Test;
import pl.put.poznan.builder.logic.Element;
import pl.put.poznan.builder.logic.TemplateBuilder;

import static org.testng.Assert.assertEquals;
import static org.testng.Assert.assertTrue;


public class TemplateBuilderTest {
    @Test
    public void builderHeadTest() {
        TemplateBuilder.Builder builder = new TemplateBuilder.Builder();
        //Element expected = new Element().setTag("head");
        //assertEquals(ja, expected);
    }


}